<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow JIT for V8 (Electron requirement) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (Electron requirement) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow DYLD environment variables (Electron debugging) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation for native addons (node-midi, uiohook-napi) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Allow incoming network connections (MIDI over network) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Accessibility API for touchpad input (CGEventTap) -->
    <key>com.apple.security.temporary-exception.accessibility</key>
    <true/>
  </dict>
</plist>
